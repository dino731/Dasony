import React, { useState } from 'react';
import './Reply.css';

const Meetball = () => {

    const handleMeetballClick = () => {

    };

    return (
        <i className={'bi bi-three-dots-vertical'} onClick={handleMeetballClick}/>
    );
};

export default Meetball;